window.SiteAnimations = (() => { function init(){ if(!('IntersectionObserver' in window))return document.querySelectorAll('.reveal').forEach(x=>x.classList.add('is-visible')); const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>observer.observe(x)); } return {init}; })();
